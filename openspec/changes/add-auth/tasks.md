## 1. Configuration and shared types

- [x] 1.1 Add `JWT_SECRET` to `REQUIRED` and a `jwt: { secret, expiresIn }` block (default `7d`) to `readEnv` in `apps/api/src/config/env.ts`, and append `JWT_SECRET=dev-only-not-a-secret` and `JWT_EXPIRES_IN=7d` to `apps/api/.env.example`. Verify the API aborts boot with `JWT_SECRET` named when the variable is unset or empty, and that a fresh `cp apps/api/.env.example apps/api/.env` still boots with no hand-editing.
- [x] 1.2 Add `packages/shared/src/auth.ts` with `AuthUser`, `DataRoom`, `AuthResponse` and `ApiError` exactly as in design.md, re-exported from `index.ts`. Verify `pnpm build --filter @dataroom/shared` emits the declarations and `pnpm typecheck` passes with the API importing `AuthResponse`.

## 2. Error envelope

- [x] 2.1 Add an `ApiExceptionFilter` registered as `APP_FILTER` plus a `ValidationPipe` `exceptionFactory` in `main.ts`, mapping the five rows of design.md's table onto `{ code, message, details? }`. Verify a body with an unknown field answers `400 VALIDATION_FAILED` with `details` naming the field, a deliberately thrown unmapped error answers `500 INTERNAL` with no stack trace or SQL in the body while the cause with its stack appears in the log, and no route can still answer in Nest's `{ statusCode, error }` shape (BR-050).

## 3. Auth module

- [x] 3.1 Add `@nestjs/jwt`, `@nestjs/passport`, `passport`, `passport-jwt`, `argon2` and `@types/passport-jwt` to `apps/api`, and add `argon2` to `onlyBuiltDependencies` in `pnpm-workspace.yaml`. Verify a clean `pnpm install` builds `argon2` without a skipped-build warning and that hashing and verifying a string round-trips in a throwaway Jest test.
- [x] 3.2 Add `AuthService` with the sign-up `$transaction` from design.md (email trimmed and lowercased, room named `<local part>'s Data Room` with the local part — never the suffix — cut to fit 255, root `FOLDER` node with `parentId: null`), sign-in with the decoy-hash verify, and the `/auth/me` lookup. Verify by unit and integration test that one sign-up leaves exactly one room holding exactly one parentless node, that a `P2002` on `User.email` surfaces as `EMAIL_TAKEN`, and that a wrong password and an unknown email produce byte-identical `401 INVALID_CREDENTIALS` bodies (FR-AUTH-010/050, BR-060).
- [x] 3.3 Add `AuthController` and its DTOs (`@IsEmail()`, `@MinLength(8)`) with `JwtModule.registerAsync` signing `{ sub, email }` for `JWT_EXPIRES_IN`. Verify `POST /api/auth/signup` → `201`, `POST /api/auth/login` → `200` and `GET /api/auth/me` → `200` against the running API, each body matching the shared `AuthResponse` / me shape, and the issued token's expiry landing 7 days out (FR-AUTH-020).
- [x] 3.4 Add `JwtStrategy` returning `{ kind: 'owner', userId }`, `JwtAuthGuard` wired as `APP_GUARD`, the `@Public()` and `@CurrentPrincipal()` decorators, and put `@Public()` on sign-up, sign-in, `GET /health` and — with a comment naming slice 4 — `GET /documents`. Verify `/auth/me` answers `401 UNAUTHENTICATED` with no header, the three public routes still answer without one, and a valid token reaches the handler as its own user (FR-AUTH-030).

## 4. Tests

- [x] 4.1 Integration tests against the compose Postgres for the account rules: sign-up happy path; a duplicate email and a case-and-whitespace variant both `409 EMAIL_TAKEN` with one row surviving; a 7-character password `400 VALIDATION_FAILED` leaving no user, room or node; a failure injected between the user and the root node leaving nothing behind; the room named from the local part, and an over-long local part cut so the name still ends in `'s Data Room`; nothing named `Root`; two concurrent sign-ups on one email yielding one `201`, one `409` and one account; and — with the Nest logger captured for the duration — no log line from any of these requests containing the submitted password or its hash (FR-AUTH-010/050, FR-ROOM-010, BR-050/060). Verify `pnpm test --filter @dataroom/api` passes.
- [x] 4.2 Guard and token tests: no header, `Bearer` alone and a `Basic` header each `401 UNAUTHENTICATED`, and an `Authorization: Share <token>` header the same, since that scheme resolves to nothing today (BR-070); an expired token, a token signed with another secret, an unsigned token and one whose header claims `alg: none`, a token with an edited payload, and a token whose user row was deleted, all likewise; a route added without any annotation refusing an anonymous call; a valid token resolving to exactly one caller (BR-010); and boot aborting with `JWT_SECRET` named when it is unset (FR-AUTH-030, BR-100). Verify `pnpm test --filter @dataroom/api` passes and no case returns a `500`.

## 5. Documentation

- [x] 5.1 Add the `INTERNAL` / `500` row to the error table in `docs/03-domain-and-api.md`, and reconcile the rest of docs/ with what shipped — the `JWT_SECRET` example value, `argon2` in `onlyBuiltDependencies`, and any endpoint or field name that came out different. Verify by re-reading docs/03 § API, § Errors and § Configuration against the merged code.
