## 1. Shared contract

- [x] 1.1 Add `packages/shared/src/nodes.ts` with `NodeType`, `FsNode`, `Breadcrumb`, `NodeStats` and `Page<T>` exactly as in design.md, re-exported from `index.ts`. Verify `pnpm build --filter @dataroom/shared` emits the declarations, `pnpm typecheck` passes with the API importing `FsNode` and `Page`, and no field is redeclared in either app.

## 2. API

- [x] 2.1 Add `NotFoundException` to `apps/api/src/http/api.exception.ts` (`404`, `ErrorCode.NOT_FOUND`, the filter's existing generic message — no argument, so no call site can vary it) and `apps/api/src/nodes/node-scope.service.ts` holding the one `resolve(principal, id)` from design.md, selecting the `FsNode` columns plus `dataRoomId` and `dataRoom.name`. Verify by integration test that a node in another owner's Data Room, an id no row has, and a malformed id all throw the same exception, and that a node in the caller's own room resolves with its `dataRoomId` and room name (BR-010, FR-ROOM-030).
- [x] 2.2 Add `apps/api/src/nodes/cursor.ts` (encode/decode the `(type, name, id)` tuple as `base64url` of `{ t, n, i }`) and `dto/list-children.query.ts` (`cursor?`, `limit` with `@Type(() => Number) @IsInt() @Min(1) @Max(100)` defaulting to 100, `type?` `@IsIn(['FOLDER','FILE'])`). Verify by unit test that encode/decode round-trips a name containing `/`, a quote and a multi-byte character, and that a truncated token, a token carrying an unknown `t`, and a token that is not base64 each throw `ValidationFailedException` with `cursor` named in `details`.
- [x] 2.3 Add `apps/api/src/nodes/node.serializer.ts` with `toFsNode` and `toNodeStats` — the change's only `BigInt` boundary: `Number(sizeBytes)`, `Number(bytes)`, `Date` → `toISOString()`. Verify by unit test that a folder row serialises with `sizeBytes` and `mimeType` null rather than absent, that a `sizeBytes` above 2^32 survives exactly, and that `JSON.stringify` of both outputs never throws.
- [x] 2.4 Add `apps/api/src/nodes/nodes.service.ts`: the keyset children page from design.md (row-value tuple predicate in `$queryRaw` — the `OR` form was reversed during apply, see design.md; `ORDER BY` qualified `type, name, id`, `LIMIT limit + 1` deciding `nextCursor`, no `count`), and the two `$queryRaw` recursive CTEs for `/path` (child to root, head segment's name replaced by `dataRoom.name`) and `/stats` (`depth > 0`, counts `::int`, bytes `::bigint`). Every query filters on the `dataRoomId` the scope service returned. Verify against the compose Postgres that a folder of 250 mixed rows walks to exhaustion in three pages with no row repeated or skipped, that a path 33 levels deep returns 33 segments outermost-first, and that stats on a file returns all zeros rather than `-1` folders.
- [x] 2.5 Add `apps/api/src/nodes/nodes.controller.ts` and `nodes.module.ts`, imported by `AppModule`: `GET /nodes/:id`, `/children`, `/path`, `/stats`, each reading `@CurrentPrincipal()` and none carrying `@Public()`. No `@IsUUID()` on `:id` — a malformed id must be `404`, not `400`. Verify against the running API that all four answer `200` for the caller's own root with bodies matching the shared types, that each answers `401 UNAUTHENTICATED` with no `Authorization` header, and that `GET /api/nodes/:id/children` on a file answers `200` with no rows and no cursor.

## 3. Tests

- [x] 3.1 Scope tests, two owners in one database: for each of the four routes, a node in the other owner's room and an id no row has both answer `404 NOT_FOUND` with byte-identical bodies, never `403`; a malformed id answers `404`, never `500`; the body names no owner email, room name or node name; a `/children` call on a foreign folder with a valid cursor returns no row from it; and the caller's own reads keep working with a second room present (BR-010, FR-ROOM-030). Verify `pnpm test --filter @dataroom/api` passes.
- [x] 3.2 Children paging tests: folders before files then name ascending; the cursor resuming exactly after the last row with no repeat or gap across a full walk; the last page reporting `nextCursor: null`; no total count anywhere in the body; `?type=FOLDER` returning folders only and still paging; `?limit=0`, `?limit=101` and `?limit=abc` each `400 VALIDATION_FAILED` naming the field; an empty folder returning no rows and no cursor; rows from a second Data Room whose names sort into the middle of the page never appearing; and an `EXPLAIN` of the query the service issues showing an index scan on `Node_dataRoomId_parentId_type_name_id_idx` with no `Sort`, no `Incremental Sort` and no `Seq Scan`, its `Index Cond` carrying both `dataRoomId` and `parentId`, the cursor tuple in `Index Cond` rather than in a `Filter`, and zero rows discarded at a cursor deep in a 4,000-row folder (FR-NAV-030). Verify `pnpm test --filter @dataroom/api` passes.
- [x] 3.3 Path and stats tests: a path running Data Room first to node last with every intervening folder present; the head segment carrying `DataRoom.name`, following a rename of that row, and no segment anywhere named `Root`; every segment's id reading back through `GET /nodes/:id`; the root's path being one segment and a file's ending at the file; stats covering a whole subtree rather than its children, excluding the node itself, returning zeros for an empty folder and for a file, summing only files, exact above 4 GiB and identical for the same files nested shallowly and 32 levels deep, and never appearing on a listing row (FR-NAV-020, FR-ROOM-010, FR-ACCT-020). Verify `pnpm test --filter @dataroom/api` passes and `pnpm typecheck && pnpm lint && pnpm test` is green from the root.

## 4. Documentation

- [x] 4.1 Reconcile `docs/` with what shipped: in `docs/03-domain-and-api.md`, correct § Recursive stats' sample SQL to the quoted camelCase columns Prisma actually emits (`"sizeBytes"`, `"dataRoomId"`, `"parentId"`) and to the `depth > 0` form that is also correct for a file, record the `?limit` maximum of 100 and the cursor's encoding in § Sorting and paging, and note that `/path`'s head segment reads `DataRoom.name`. Verify by re-reading § Model, § Sorting and paging, § Recursive stats and § API against the merged code, and drop this task if nothing diverged.
